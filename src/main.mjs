src / main.js;

// Replace all import statements with require statements
const fs = require("fs");
const path = require("path");
const express = require("express");

// Rest of the code...

// Change the exporting format from export default to module.exports
module.exports = {
  // Exported functions and variables...
};
